# Common settings
EDITOR=nvim
BROWSER=firefox

# PATH settings
PATH DEFAULT=${PATH}:@{HOME}/.cargo/bin
PATH DEFAULT=${PATH}:@{HOME}/.local/bin
PATH DEFAULT=${PATH}:@{HOME}/.npm-packages/bin

# Rust options
CARGO_TARGET_DIR DEFAULT=@{HOME}/.cargo-target
CARGO_INCREMENTAL=1
RUSTFLAGS DEFAULT="-C target-cpu=native"
RUST_BACKTRACE=1

# Miscellaneous
LESS DEFAULT="-F -X -R"
